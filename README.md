– Automating the RedBus ticket booking process involves creating a system that can perform the entire ticket
  booking procedure with minimal human intervention
– The algorithm is designed in such a waty that it will always book window seats.
– It avoids doing monotonus tasks of booking tickets
